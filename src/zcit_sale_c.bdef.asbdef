projection;
strict ( 2 );
use draft;

define behavior for ZCIT_SALE_C alias SalesOrderHdr
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;

  use association _salesitem { create; with draft; }
}

define behavior for ZCIT_SALE_O_C alias SalesOrderItm
{
  use update;
  use delete;

  use association _salesHeader { with draft; }
}